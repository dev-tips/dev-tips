extends layout.pug

block content
  +article('question')
    +articleHeadline(title)
    +articleMeta(formatDate(date))
      +articleDate(date)
      +articleContributors(contributors)
    +articleContent()
      | !{shortcodes(markdown(answer))}

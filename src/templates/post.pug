extends layout.pug

block content
  +article()
    +articleHeadline(title)
    +articleMeta(formatDate(date))
      +articleDate(date)
      +articleContributors(contributors)
    +articleContent()
      | !{shortcodes(markdown(text))}
